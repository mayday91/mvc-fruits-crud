{% layout 'layout.liquid' %}

{% block content %}
<div>
    {% for fruit in fruits %}
    <a href="/fruits/{{fruit._id}}">{{fruit.name}}</a>
    <p>Color: {{fruit.color}}</p>
    
    {% endfor %}
</div>
{% endblock %}